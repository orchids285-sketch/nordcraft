{
  "name": "nordcraft-backend",
  "type": "module",
  "scripts": {
    "cf:types": "wrangler types",
    "compile": "bun build --minify --target=node ./src/node.index.ts --outdir dist/",
    "build": "cd ../../ && bun run build",
    "sync": "bun bin/syncStaticAssets.ts",
    "buildandsync": "bun run build && bun run sync",
    "predocker:build": "bun run buildandsync",
    "docker:build": "cd ../../ && docker build . -t nordcraft-backend -f packages/backend/Dockerfile",
    "predocker:build:debug": "bun run buildandsync",
    "docker:build:debug": "cd ../../ && docker build --no-cache --progress=plain . -t nordcraft-backend -f packages/backend/Dockerfile",
    "docker:run:debug": "docker rm -f nordcraft-backend && docker run --rm -p 3000:3000 --name nordcraft-backend -e PORT=3000 nordcraft-backend",
    "docker:run": "docker run -p 3000:3000 --name nordcraft-backend -e PORT=3000 nordcraft-backend",
    "predev": "bun run sync && bunx esbuild --bundle --outdir=dist --platform=browser --format=esm src/index.ts",
    "dev": "wrangler dev --no-bundle --port 8989",
    "prenoderun": "bun run buildandsync",
    "noderun": "bun build --compile --minify --sourcemap src/node.index.ts --outfile dist/nordcraft-backend && cd dist/ && ./nordcraft-backend",
    "prebunrun": "bun run buildandsync",
    "bunrun": "bun build --compile --minify --sourcemap src/bun.index.ts --outfile dist/nordcraft-backend && cd dist/ && ./nordcraft-backend",
    "preview-link": "sh -c 'ln -s \"$1\" ./nordcraft-internal' --",
    "prepreview": "bun run sync",
    "preview": "wrangler dev --port 8989 -c preview.wrangler.toml -c ./nordcraft-internal/packages/worker/wrangler.toml -c ./nordcraft-internal/packages/worker-tail/wrangler.toml",
    "predeploy-preview": "bun run sync && bunx esbuild --bundle --outdir=dist --platform=browser --format=esm src/preview.index.ts",
    "deploy-preview": "wrangler deploy -c preview.wrangler.toml",
    "predeploy": "bun run sync && bunx esbuild --bundle --outdir=dist --platform=browser --format=esm src/index.ts",
    "deploy": "wrangler deploy --no-bundle",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:watch:only": "bun test --watch --only",
    "typecheck": "tsgo --noEmit",
    "typecheck:watch": "tsgo --noEmit --watch",
    "watch": "tsgo --noEmit -w"
  },
  "dependencies": {
    "@hono/node-server": "1.19.11",
    "@nordcraft/core": "workspace:*",
    "@nordcraft/ssr": "workspace:*",
    "hono": "4.12.26",
    "nanoid": "5.1.16"
  },
  "devDependencies": {
    "@nordcraft/runtime": "workspace:*",
    "@types/node": "26.1.0",
    "wrangler": "4.110.0"
  }
}
